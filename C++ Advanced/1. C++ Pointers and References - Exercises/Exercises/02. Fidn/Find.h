#ifndef FIND_H
#define FIND_H

#include<vector>
#include<Company.h>

Company* find(std::vector<Company*> v, int id)
{
	Company* found = nullptr;
	Company* it = v.begin();

	return vector.begin();
}

#endif // !FIND_H
